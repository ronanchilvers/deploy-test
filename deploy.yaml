---
shared:
  folders:
    - shared
  files:
    - shared.yaml
writables:
  after:
    - echo "foobar foobar"
  paths:
    - writable
clear_paths:
  paths:
    - delete.txt
activate:
  after:
    - php     -v
