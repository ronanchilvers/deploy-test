---
shared:
  folders:
    - shared
  files:
    - shared.yaml
writables:
  after:
   - php -r 'echo "writable after hook\n";'
  paths:
    - writable
clear_paths:
  paths:
    - delete.txt
activate:
  after:
   - php -r 'echo "activate after hook\n";'
