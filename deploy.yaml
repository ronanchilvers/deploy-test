---
shared:
  folders:
    - shared
  files:
    - shared.yaml
writables:
  after:
   - /usr/local/bin/php -r 'echo "writable after hook\n";'
  paths:
    - writable
clear_paths:
  paths:
    - delete.txt
activate:
  after:
   - /usr/local/bin/php -r 'echo "activate after hook\n";'
