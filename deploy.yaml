---
shared:
  folders:
    - shared
  files:
    - shared.yaml
writables:
  after:
    - php -r 'echo "foobar\n";'
  paths:
    - writable
clear_paths:
  paths:
    - delete.txt
activate:
  after:
    - php     -v
